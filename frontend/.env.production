# Baked into the build by Vite (`vite build`). Points the deployed UI at the
# Cloud Run backend so register/login hit the real API + Neon DB.
VITE_API_BASE=https://learnflow-auth-596131598077.us-east1.run.app
