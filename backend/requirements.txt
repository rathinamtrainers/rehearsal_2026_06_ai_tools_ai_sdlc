fastapi==0.138.1
# Pinned explicitly to keep the transitive Starlette off the vulnerable <0.42
# line that fastapi 0.115.6 forced (Snyk: High ReDoS/SSRF, Medium request-smuggling).
# fastapi 0.138.1 requires starlette>=0.46.0; 1.3.1 is the current patched release.
starlette==1.3.1
uvicorn[standard]==0.34.0
SQLAlchemy==2.0.36
alembic==1.14.0
pydantic[email]==2.10.4
pydantic-settings==2.7.0
pwdlib[bcrypt]==0.2.1
PyJWT==2.13.0
# Postgres driver for Cloud SQL (prod). SQLite (dev) needs no driver.
psycopg2-binary==2.9.10
